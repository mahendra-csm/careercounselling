import AssessmentFlow from '@/components/assessment/AssessmentFlow';

export const metadata = {
  title: 'Career Assessment — OneGrasp',
};

export default function AssessmentPage() {
  return <AssessmentFlow />;
}
